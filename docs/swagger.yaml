openapi: 3.0.3

info:
  title: Р Р°СЃС‡РµС‚ СЃРµР±РµСЃС‚РѕРёРјРѕСЃС‚Рё РїСЂРё РїРѕР·Р°РєР°Р·РЅРѕРј РїСЂРѕРёР·РІРѕРґСЃС‚РІРµ
  description: API РґР»СЏ Р·Р°РіСЂСѓР·РєРё CSV, СЂР°СЃС‡РµС‚Р° СЃРµР±РµСЃС‚РѕРёРјРѕСЃС‚Рё, СЂР°Р±РѕС‚С‹ СЃ Р·Р°РєР°Р·Р°РјРё, Р°РІС‚РѕСЂРёР·Р°С†РёРё Рё С„РѕСЂРјРёСЂРѕРІР°РЅРёСЏ РѕС‚С‡РµС‚РѕРІ.
  version: 1.0.0

servers:
  - url: http://localhost:8080
    description: Р›РѕРєР°Р»СЊРЅС‹Р№ СЃРµСЂРІРµСЂ

tags:
  - name: Auth
    description: Р РµРіРёСЃС‚СЂР°С†РёСЏ Рё Р°РІС‚РѕСЂРёР·Р°С†РёСЏ РїРѕР»СЊР·РѕРІР°С‚РµР»РµР№
  - name: Uploads
    description: Р—Р°РіСЂСѓР·РєР° CSV-С„Р°Р№Р»РѕРІ
  - name: Orders
    description: Р—Р°РєР°Р·С‹ Рё РґР°РЅРЅС‹Рµ РїРѕ Р·Р°РєР°Р·Р°Рј
  - name: Calculations
    description: Р Р°СЃС‡РµС‚С‹ РїРѕ CSV-С„Р°Р№Р»Р°Рј
  - name: Reports
    description: Р¤РѕСЂРјРёСЂРѕРІР°РЅРёРµ Рё СЃРєР°С‡РёРІР°РЅРёРµ РѕС‚С‡РµС‚РѕРІ
  - name: Mocks
    description: РўРµСЃС‚РѕРІС‹Рµ РґР°РЅРЅС‹Рµ

paths:
  /api/auth/register:
    post:
      tags: [Auth]
      summary: Р—Р°СЂРµРіРёСЃС‚СЂРёСЂРѕРІР°С‚СЊ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ
      operationId: registerUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthRequest'
      responses:
        '201':
          description: РџРѕР»СЊР·РѕРІР°С‚РµР»СЊ СЃРѕР·РґР°РЅ
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateEntityResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '409':
          $ref: '#/components/responses/ConflictText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /api/auth/login:
    post:
      tags: [Auth]
      summary: РџРѕР»СѓС‡РёС‚СЊ JWT-С‚РѕРєРµРЅ
      operationId: loginUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthRequest'
      responses:
        '200':
          description: РђРІС‚РѕСЂРёР·Р°С†РёСЏ РІС‹РїРѕР»РЅРµРЅР°
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '401':
          $ref: '#/components/responses/UnauthorizedText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /upload:
    post:
      tags: [Uploads]
      summary: Р—Р°РіСЂСѓР·РёС‚СЊ CSV-С„Р°Р№Р» РІ Р±Р°Р·Сѓ РґР°РЅРЅС‹С…
      description: Р—Р°РіСЂСѓР¶Р°РµС‚ РѕРґРёРЅ CSV-С„Р°Р№Р» С‚РёРїР° boms, labor РёР»Рё overhead. РўСЂРµР±СѓРµС‚СЃСЏ JWT-С‚РѕРєРµРЅ.
      operationId: uploadFile
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [type, file]
              properties:
                type:
                  type: string
                  enum: [boms, labor, overhead]
                  description: РўРёРї Р·Р°РіСЂСѓР¶Р°РµРјС‹С… РґР°РЅРЅС‹С…
                file:
                  type: string
                  format: binary
                  description: CSV-С„Р°Р№Р»
      responses:
        '200':
          description: Р¤Р°Р№Р» СѓСЃРїРµС€РЅРѕ Р·Р°РіСЂСѓР¶РµРЅ
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '401':
          $ref: '#/components/responses/UnauthorizedText'
        '405':
          $ref: '#/components/responses/MethodNotAllowedText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders:
    get:
      tags: [Orders]
      summary: РџРѕР»СѓС‡РёС‚СЊ СЃРїРёСЃРѕРє Р·Р°РєР°Р·РѕРІ
      operationId: getOrders
      responses:
        '200':
          description: РЎРїРёСЃРѕРє Р·Р°РєР°Р·РѕРІ
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrderResponse'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'
    post:
      tags: [Orders]
      summary: РЎРѕР·РґР°С‚СЊ Р·Р°РєР°Р·
      operationId: createOrder
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrderRequest'
      responses:
        '201':
          description: Р—Р°РєР°Р· СЃРѕР·РґР°РЅ
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateEntityResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '401':
          $ref: '#/components/responses/UnauthorizedText'
        '409':
          $ref: '#/components/responses/ConflictText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders/{id}:
    get:
      tags: [Orders]
      summary: РџРѕР»СѓС‡РёС‚СЊ Р·Р°РєР°Р· РїРѕ ID
      operationId: getOrderById
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Р”Р°РЅРЅС‹Рµ Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '404':
          $ref: '#/components/responses/NotFoundText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders/{id}/cost/{method}:
    get:
      tags: [Orders]
      summary: Р Р°СЃСЃС‡РёС‚Р°С‚СЊ СЃРµР±РµСЃС‚РѕРёРјРѕСЃС‚СЊ Р·Р°РєР°Р·Р°
      operationId: getOrderCost
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - name: method
          in: path
          required: true
          schema:
            type: string
            enum: [bom, labor]
          description: РњРµС‚РѕРґ СЂР°СЃРїСЂРµРґРµР»РµРЅРёСЏ РЅР°РєР»Р°РґРЅС‹С… СЂР°СЃС…РѕРґРѕРІ
      responses:
        '200':
          description: РЎРµР±РµСЃС‚РѕРёРјРѕСЃС‚СЊ Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders/{id}/changes:
    get:
      tags: [Orders]
      summary: РџРѕР»СѓС‡РёС‚СЊ РёСЃС‚РѕСЂРёСЋ Р·Р°РіСЂСѓР·РѕРє РїРѕ Р·Р°РєР°Р·Сѓ
      operationId: getOrderChanges
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Р–СѓСЂРЅР°Р» РёР·РјРµРЅРµРЅРёР№ Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UploadLog'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders/{id}/boms:
    get:
      tags: [Orders]
      summary: РџРѕР»СѓС‡РёС‚СЊ BOM-РїРѕР·РёС†РёРё Р·Р°РєР°Р·Р°
      operationId: getOrderBoms
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: BOM-РїРѕР·РёС†РёРё Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BOMItemResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders/{id}/labor:
    get:
      tags: [Orders]
      summary: РџРѕР»СѓС‡РёС‚СЊ С‚СЂСѓРґРѕР·Р°С‚СЂР°С‚С‹ Р·Р°РєР°Р·Р°
      operationId: getOrderLabor
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: РўСЂСѓРґРѕР·Р°С‚СЂР°С‚С‹ Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LaborItemResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /orders/{id}/overhead:
    get:
      tags: [Orders]
      summary: РџРѕР»СѓС‡РёС‚СЊ РЅР°РєР»Р°РґРЅС‹Рµ СЂР°СЃС…РѕРґС‹ Р·Р°РєР°Р·Р°
      operationId: getOrderOverhead
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: РќР°РєР»Р°РґРЅС‹Рµ СЂР°СЃС…РѕРґС‹ Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OverheadItemResponse'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /api/calculate:
    post:
      tags: [Calculations]
      summary: Рассчитать себестоимость по трем CSV-файлам
      description: Выполняет расчет по BOM, Labor и Overhead, учитывает overheadMethod, сохраняет расчет и возвращает его ID. JWT-токен необязателен.
      operationId: calculateFromFiles
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [bom, labor, overhead]
              properties:
                bom:
                  type: string
                  format: binary
                  description: CSV-файл состава заказа
                labor:
                  type: string
                  format: binary
                  description: CSV-файл трудозатрат
                overhead:
                  type: string
                  format: binary
                  description: CSV-файл накладных расходов
                overheadMethod:
                  type: string
                  enum: [direct_costs, labor_hours]
                  default: direct_costs
                  description: Метод распределения накладных расходов
                orderNumber:
                  type: string
                  description: Опциональный ID заказа для расчета по конкретному заказу
      responses:
        '200':
          description: Расчет выполнен
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalculationResult'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '405':
          $ref: '#/components/responses/MethodNotAllowedText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /api/report/generate:
    post:
      tags: [Reports]
      summary: Старый триггер формирования отчета через 1С
      description: Вызывает метод 1С пустым POST с Content-Length: 0. 1С сама забирает данные через /orders и /orders/{id}/boms|labor|overhead. В ответ возвращается Excel-файл.
      operationId: generateAPIReport
      responses:
        '200':
          description: Excel-файл отчета
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '502':
          $ref: '#/components/responses/BadGatewayJSON'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /api/generate-excel:
    post:
      tags: [Reports]
      summary: Сформировать Excel из JSON отчета 1С
      description: 1С отправляет сформированный JSON отчета, сервер возвращает report.xlsx.
      operationId: generateExcelFromOneC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReportRequest'
      responses:
        '200':
          description: Excel-файл
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequestText'
        '405':
          $ref: '#/components/responses/MethodNotAllowedText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /reports/generate:
    post:
      tags: [Reports]
      summary: Запросить отчет у внешней системы 1С
      description: По идентификатору сохраненного расчета вызывает метод 1С пустым POST с Content-Length: 0, сохраняет ответ 1С и возвращает Excel-файл. JWT-токен необязателен.
      operationId: generateReport
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateReportRequest'
      responses:
        '200':
          description: Excel-файл отчета
          headers:
            Content-Disposition:
              schema:
                type: string
                example: attachment; filename=report.xlsx
            X-Report-ID:
              schema:
                type: integer
              description: ID сохраненного отчета
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequestText'
        '404':
          $ref: '#/components/responses/NotFoundText'
        '405':
          $ref: '#/components/responses/MethodNotAllowedText'
        '502':
          $ref: '#/components/responses/BadGatewayJSON'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /reports/{id}/excel:
    get:
      tags: [Reports]
      summary: РЎРєР°С‡Р°С‚СЊ Excel-С„Р°Р№Р» СЃРѕС…СЂР°РЅРµРЅРЅРѕРіРѕ РѕС‚С‡РµС‚Р°
      operationId: downloadReportExcel
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
          description: ID РѕС‚С‡РµС‚Р°
      responses:
        '200':
          description: Excel-С„Р°Р№Р» РѕС‚С‡РµС‚Р°
          headers:
            Content-Disposition:
              schema:
                type: string
                example: attachment; filename=report.xlsx
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequestText'
        '404':
          $ref: '#/components/responses/NotFoundText'
        '500':
          $ref: '#/components/responses/InternalServerErrorText'

  /mocks/orders:
    get:
      tags: [Mocks]
      summary: РџРѕР»СѓС‡РёС‚СЊ С‚РµСЃС‚РѕРІС‹Р№ СЃРїРёСЃРѕРє Р·Р°РєР°Р·РѕРІ
      operationId: getMockOrders
      responses:
        '200':
          description: РўРµСЃС‚РѕРІС‹Рµ Р·Р°РєР°Р·С‹
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MockOrder'

  /mocks/orders/{id}/cost:
    get:
      tags: [Mocks]
      summary: РџРѕР»СѓС‡РёС‚СЊ С‚РµСЃС‚РѕРІСѓСЋ СЃРµР±РµСЃС‚РѕРёРјРѕСЃС‚СЊ Р·Р°РєР°Р·Р°
      operationId: getMockOrderCost
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: РўРµСЃС‚РѕРІР°СЏ СЃРµР±РµСЃС‚РѕРёРјРѕСЃС‚СЊ Р·Р°РєР°Р·Р°
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockOrderCost'
        '400':
          $ref: '#/components/responses/BadRequestText'
        '404':
          $ref: '#/components/responses/NotFoundText'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    OrderId:
      name: id
      in: path
      required: true
      schema:
        type: integer
        minimum: 1
      description: РРґРµРЅС‚РёС„РёРєР°С‚РѕСЂ Р·Р°РєР°Р·Р°

  responses:
    BadRequestText:
      description: РќРµРєРѕСЂСЂРµРєС‚РЅС‹Р№ Р·Р°РїСЂРѕСЃ
      content:
        text/plain:
          schema:
            type: string
    UnauthorizedText:
      description: РўСЂРµР±СѓРµС‚СЃСЏ Р°РІС‚РѕСЂРёР·Р°С†РёСЏ РёР»Рё РїРµСЂРµРґР°РЅ РЅРµРІРµСЂРЅС‹Р№ С‚РѕРєРµРЅ
      content:
        text/plain:
          schema:
            type: string
    NotFoundText:
      description: РћР±СЉРµРєС‚ РЅРµ РЅР°Р№РґРµРЅ
      content:
        text/plain:
          schema:
            type: string
    ConflictText:
      description: РљРѕРЅС„Р»РёРєС‚ РґР°РЅРЅС‹С…
      content:
        text/plain:
          schema:
            type: string
    MethodNotAllowedText:
      description: РњРµС‚РѕРґ РЅРµ РїРѕРґРґРµСЂР¶РёРІР°РµС‚СЃСЏ
      content:
        text/plain:
          schema:
            type: string
    BadGatewayText:
      description: РћС€РёР±РєР° РІРЅРµС€РЅРµРіРѕ СЃРµСЂРІРёСЃР°
      content:
        text/plain:
          schema:
            type: string
    BadGatewayJSON:
      description: Ошибка внешнего сервиса
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/OneCError'
    InternalServerErrorText:
      description: Р’РЅСѓС‚СЂРµРЅРЅСЏСЏ РѕС€РёР±РєР° СЃРµСЂРІРµСЂР°
      content:
        text/plain:
          schema:
            type: string

  schemas:
    AuthRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          example: user@example.com
        password:
          type: string
          format: password
          example: password123

    StatusResponse:
      type: object
      properties:
        status:
          type: string
          example: ok

    CreateEntityResponse:
      type: object
      properties:
        status:
          type: string
          example: ok
        id:
          type: integer
          example: 101

    LoginResponse:
      type: object
      properties:
        status:
          type: string
          example: ok
        token:
          type: string
          description: JWT-С‚РѕРєРµРЅ РґР»СЏ Р·Р°РіРѕР»РѕРІРєР° Authorization: Bearer {token}

    CreateOrderRequest:
      type: object
      required: [id, start_date, status]
      properties:
        id:
          type: integer
          example: 101
        start_date:
          type: string
          format: date
          example: '2026-03-09'
        end_date:
          type: string
          format: date
          nullable: true
          example: '2026-03-10'
        status:
          type: string
          example: created

    OrderResponse:
      type: object
      properties:
        id:
          type: integer
          example: 101
        start_date:
          type: string
          format: date-time
        end_date:
          type: string
          format: date-time
          nullable: true
        total_cost:
          type: number
          format: double
          example: 126.25
        status:
          type: string
          example: created
        error_id:
          type: integer
          nullable: true

    CostResponse:
      type: object
      properties:
        materials:
          type: number
          format: double
          example: 80.5
        labor:
          type: number
          format: double
          example: 30
        overhead:
          type: number
          format: double
          example: 15.75
        total:
          type: number
          format: double
          example: 126.25

    CalculationResult:
      type: object
      properties:
        calculationId:
          type: integer
          example: 1
        bomCost:
          type: number
          format: double
          example: 80.5
        laborCost:
          type: number
          format: double
          example: 30
        overheadCost:
          type: number
          format: double
          example: 15.75
        totalCost:
          type: number
          format: double
          example: 126.25

    BOMItemResponse:
      type: object
      properties:
        id:
          type: integer
        order_id:
          type: integer
        quantity:
          type: number
          format: double
        unit_cost:
          type: number
          format: double
        material_code:
          type: string

    LaborItemResponse:
      type: object
      properties:
        id:
          type: integer
        order_id:
          type: integer
        rate:
          type: number
          format: double
        hours:
          type: number
          format: double

    OverheadItemResponse:
      type: object
      properties:
        id:
          type: integer
        order_id:
          type: integer
        date:
          type: string
          example: '2026-03-09'
        prod_type:
          type: string
        amount:
          type: number
          format: double

    UploadLog:
      type: object
      properties:
        id:
          type: integer
        order_id:
          type: integer
        file_type:
          type: string
          enum: [boms, labor, overhead]
        uploaded_at:
          type: string
          format: date-time
        changed_by:
          type: string

    GenerateReportRequest:
      type: object
      required: [calculation_id]
      properties:
        calculation_id:
          type: integer
          description: ID СЃРѕС…СЂР°РЅРµРЅРЅРѕРіРѕ СЂР°СЃС‡РµС‚Р°
          example: 1
        calculationId:
          type: integer
          description: РђР»СЊС‚РµСЂРЅР°С‚РёРІРЅРѕРµ РёРјСЏ РїРѕР»СЏ, РєРѕС‚РѕСЂРѕРµ С‚Р°РєР¶Рµ РїРѕРґРґРµСЂР¶РёРІР°РµС‚СЃСЏ СЃРµСЂРІРµСЂРѕРј
          example: 1

    ReportRequest:
      type: object
      properties:
        status:
          type: string
          example: ok
        date:
          type: array
          items:
            $ref: '#/components/schemas/ReportItem'

    ReportItem:
      type: object
      properties:
        order:
          type: string
          example: '101'
        type:
          type: string
          example: materials
        sum:
          type: number
          format: double
          example: 80.5

    OneCError:
      type: object
      properties:
        message:
          type: string
          example: Ошибка соединения с 1С
        request_url:
          type: string
          example: https://example/InfoBase/hs/api/generate
        request_body:
          type: string
          example: ''
        onec_status_code:
          type: integer
          example: 500
        onec_status:
          type: string
          example: 500 Internal Server Error
        onec_body:
          type: string
        cause:
          type: string
          example: EOF
        timestamp:
          type: string
          format: date-time

    MockOrder:
      type: object
      properties:
        id:
          type: integer
        start_date:
          type: string
          example: 09-03-2026
        end_date:
          type: string
          example: 10-03-2026
        total_cost:
          type: integer
        status:
          type: string
        error_id:
          type: integer
          nullable: true

    MockOrderCost:
      type: object
      properties:
        order_id:
          type: integer
        materials:
          type: number
          format: double
        labor:
          type: number
          format: double
        overhead:
          type: number
          format: double
        total:
          type: number
          format: double
